
Система CheckTransmit в Source 2 позволяет контролировать, какие объекты передаются по сети конкретным игрокам. Эта мощная функция позволяет создавать продвинутые игровые механики, такие как скрытие/показ объектов, создание правил видимости для конкретных игроков и оптимизация сетевого трафика.

[quote][b]Info:[/b] [b]Что такое CheckTransmit?[/b]: CheckTransmit вызывается каждый тик для определения, какие объекты должны быть переданы (по сети) каждому игроку. Подключившись к этой системе, вы можете динамически контролировать, что видит каждый игрок.[/quote]

[size=20][b]Обзор[/b][/size]

Для работы с системой CheckTransmit необходимо:

[list=1]
[*][b]Зарегистрировать хук[/b] с помощью [font=monospace]OnServerCheckTransmit_Register[/font] при запуске плагина
[*][b]Получать указатели CheckTransmitInfo[/b] для каждого игрока в вашем колбэке
[*][b]Изменять состояния передачи[/b] используя предоставленные методы
[*][b]Отменить регистрацию хука[/b] с помощью [font=monospace]OnServerCheckTransmit_Unregister[/font] при завершении плагина
[/list]

[quote][b]Info:[/b] [b]Предупреждение о производительности[/b]: CheckTransmit вызывается [b]каждый тик[/b] для каждого игрока. Делайте логику вашего колбэка эффективной, чтобы избежать проблем с производительностью сервера.[/quote]

[size=20][b]Базовая настройка[/b][/size]

Зарегистрируйте хук CheckTransmit в методе запуска вашего плагина и отмените регистрацию при завершении:

[spoiler=c#]
[code=csharp]  using Plugify;
  using static s2sdk.s2sdk;

  public unsafe class Sample : Plugin
  {
      public void OnPluginStart()
      {
          OnServerCheckTransmit_Register(OnCheckTransmit);
      }

      public void OnPluginEnd()
      {
          OnServerCheckTransmit_Unregister(OnCheckTransmit);
      }

      private void OnCheckTransmit(nint[] infos)
      {
          // индекс массива infos представляет слот игрока (0-63)
          for (int playerSlot = 0; playerSlot < infos.Length; playerSlot++)
          {
              var info = infos[playerSlot];
              // Изменяйте состояния передачи для этого слота игрока
          }
      }
  }
  [font=monospace]`[/font]
[/spoiler]

[spoiler=c++][/code]cpp
  #include <plg/plugin.hpp>
  #include "s2sdk.hpp"

  using namespace s2sdk;

  class Sample : public plg::IPluginEntry {
  public:
      void OnPluginStart() override {
          OnServerCheckTransmit_Register(
              [](const plg::vector<void*>& infos) {
                  // индекс массива infos представляет слот игрока (0-63)
                  for (size_t playerSlot = 0; playerSlot < infos.size(); playerSlot++) {
                      auto* info = infos[playerSlot];
                      // Изменяйте состояния передачи для этого слота игрока
                  }
              });
      }

      void OnPluginEnd() override {
          OnServerCheckTransmit_Unregister();
      }
  };
  [code][/spoiler]

[spoiler=python][/code]python
  from plugify.plugin import Plugin
  from plugify.pps import s2sdk as s2

  class Sample(Plugin):
      def plugin_start(self):
          s2.OnServerCheckTransmit_Register(self.on_check_transmit)

      def plugin_end(self):
          s2.OnServerCheckTransmit_Unregister(self.on_check_transmit)

      def on_check_transmit(self, infos):
          # индекс массива infos представляет слот игрока (0-63)
          for player_slot, info in enumerate(infos):
              # Изменяйте состояния передачи для этого слота игрока
              pass
  [code][/spoiler]

[spoiler=go][/code]go
  package main

  import (
      "s2sdk"
      "github.com/untrustedmodders/go-plugify"
  )

  func init() {
      plugify.OnPluginStart(func() {
          s2sdk.OnServerCheckTransmit_Register(func(infos []s2sdk.CCheckTransmitInfo) {
              // индекс массива infos представляет слот игрока (0-63)
              for playerSlot, info := range infos {
                  // Изменяйте состояния передачи для этого слота игрока
                  _ = playerSlot
                  _ = info
              }
          })
      })

      plugify.OnPluginEnd(func() {
          s2sdk.OnServerCheckTransmit_Unregister()
      })
  }
  [code][/spoiler]

[spoiler=js][/code]js
  import { Plugin } from 'plugify';
  import { s2 } from ':s2sdk';

  export class Sample extends Plugin {
      pluginStart() {
          s2.OnServerCheckTransmit_Register((infos) => {
              // индекс массива infos представляет слот игрока (0-63)
              infos.forEach((info, playerSlot) => {
                  // Изменяйте состояния передачи для этого слота игрока
              });
          });
      }

      pluginEnd() {
          s2.OnServerCheckTransmit_Unregister();
      }
  }
  [code][/spoiler]

[spoiler=lua][/code]lua
  local plugify = require 'plugify'
  local Plugin = plugify.Plugin
  local s2 = require 's2sdk'

  local Sample = {}
  setmetatable(Sample, { __index = Plugin })

  function Sample:plugin_start()
      s2:OnServerCheckTransmit_Register(function(infos)
          -- индекс массива infos представляет слот игрока (0-63)
          for playerSlot, info in ipairs(infos) do
              -- Изменяйте состояния передачи для этого слота игрока
          end
      end)
  end

  function Sample:plugin_end()
      s2:OnServerCheckTransmit_Unregister()
  end

  local M = {}
  M.Sample = Sample
  return M
  [code][/spoiler]
[size=20][b]Структура CheckTransmitInfo[/b][/size]

Каждый объект [font=monospace]CheckTransmitInfo[/font] содержит информацию о состояниях передачи для конкретного игрока. [b]Слот игрока определяется индексом массива[/b] (0-63), а не полем в структуре.

[list]
[*][b]m_pTransmitEntity[/b]: BitVec, контролирующий, какие объекты передаются
[*][b]m_pTransmitNonPlayers[/b]: BitVec для объектов, не являющихся игроками
[*][b]m_pTransmitAlways[/b]: BitVec для объектов, которые всегда должны передаваться
[*][b]m_vecTargetSlots[/b]: Список целевых слотов игроков
[*][b]m_bFullUpdate[/b]: Должно ли быть отправлено полное обновление
[/list]

[quote][b]Info:[/b] [b]Слот игрока[/b]: Позиция CheckTransmitInfo в массиве infos представляет слот игрока. [font=monospace]infos[0][/font] для слота игрока 0, [font=monospace]infos[1][/font] для слота игрока 1, и т.д.[/quote]

[quote][b]Info:[/b] [b]Объяснение BitVec[/b]: BitVec - это эффективная структура данных, которая использует отдельные биты для отслеживания состояний объектов. Каждая позиция бита соответствует индексу объекта.[/quote]

[size=20][b]Доступные методы[/b][/size]

S2SDK предоставляет два способа работы с CheckTransmitInfo:

[size=16][b]1. Функциональный API (прямые вызовы методов)[/b][/size]

Используйте эти методы, передавая указатель CheckTransmitInfo в качестве первого параметра:

#### Методы передачи объектов[/code]csharp
SetTransmitInfoEntity(info, entityHandle);          // Отметить объект как передаваемый
ClearTransmitInfoEntity(info, entityHandle);        // Отметить объект как не передаваемый
IsTransmitInfoEntitySet(info, entityHandle);        // Проверить, передается ли объект
SetTransmitInfoEntityAll(info);                     // Отметить все объекты как передаваемые
ClearTransmitInfoEntityAll(info);                   // Отметить все объекты как не передаваемые
[code]
#### Методы передачи не-игроков[/code]csharp
SetTransmitInfoNonPlayer(info, entityHandle);       // Отметить не-игрока как передаваемого
ClearTransmitInfoNonPlayer(info, entityHandle);     // Отметить не-игрока как не передаваемого
IsTransmitInfoNonPlayerSet(info, entityHandle);     // Проверить, передается ли не-игрок
SetTransmitInfoNonPlayerAll(info);                  // Отметить всех не-игроков как передаваемых
ClearTransmitInfoNonPlayerAll(info);                // Отметить всех не-игроков как не передаваемых
[code]
#### Методы постоянной передачи[/code]csharp
SetTransmitInfoAlways(info, entityHandle);          // Отметить объект для постоянной передачи
ClearTransmitInfoAlways(info, entityHandle);        // Снять отметку объекта с постоянной передачи
IsTransmitInfoAlwaysSet(info, entityHandle);        // Проверить, всегда ли передается объект
SetTransmitInfoAlwaysAll(info);                     // Отметить все объекты для постоянной передачи
ClearTransmitInfoAlwaysAll(info);                   // Снять отметку всех объектов с постоянной передачи
[code]
#### Методы целевых слотов[/code]csharp
GetTransmitInfoTargetSlotsCount(info);              // Получить количество целевых слотов
GetTransmitInfoTargetSlot(info, index);             // Получить конкретный целевой слот
AddTransmitInfoTargetSlot(info, playerSlot);        // Добавить целевой слот
RemoveTransmitInfoTargetSlot(info, index);          // Удалить целевой слот по индексу
GetTransmitInfoTargetSlotsAll(info);                // Получить все целевые слоты
RemoveTransmitInfoTargetSlotsAll(info);             // Очистить все целевые слоты
[code]
#### Методы полного обновления[/code]csharp
GetTransmitInfoFullUpdate(info);                    // Получить флаг полного обновления
SetTransmitInfoFullUpdate(info, fullUpdate);        // Установить флаг полного обновления
[code]
[size=16][b]2. Объектно-ориентированный API (класс CheckTransmitInfo)[/b][/size]

В языках, которые это поддерживают, вы можете создать класс CheckTransmitInfo из указателя для более интуитивного доступа в стиле ООП:

[spoiler=c#][/code]csharp
  private void OnCheckTransmit(nint[] infos)
  {
      for (int playerSlot = 0; playerSlot < infos.Length; playerSlot++)
      {
          // Создать OOP обёртку из указателя
          var info = new CheckTransmitInfo(infos[playerSlot]);

          // Использовать методы объекта вместо статических функций
          info.SetEntity(entityHandle);
          info.ClearEntity(entityHandle);
          bool isSet = info.IsEntitySet(entityHandle);

          // Примечание: playerSlot берётся из индекса массива, а не из метода
          info.SetFullUpdate(true);
      }
  }
  [code][/spoiler]

[spoiler=c++][/code]cpp
  void OnCheckTransmit(const plg::vector<void*>& infos) {
      for (size_t playerSlot = 0; playerSlot < infos.size(); playerSlot++) {
          // Использовать указатель напрямую с методами
          CheckTransmitInfo info(infos[playerSlot]);

          info.SetEntity(entityHandle);
          info.ClearEntity(entityHandle);
          bool isSet = info.IsEntitySet(entityHandle);

          // Примечание: playerSlot берётся из индекса массива, а не из метода
          info.SetFullUpdate(true);
      }
  }
  [code][/spoiler]
[size=20][b]Практические примеры[/b][/size]

[size=16][b]Пример 1: Скрыть конкретный объект от всех игроков[/b][/size]

[spoiler=c#][/code]csharp
  using Plugify;
  using static s2sdk.s2sdk;

  public unsafe class HideEntity : Plugin
  {
      private int hiddenEntityHandle = -1;

      public void OnPluginStart()
      {
          OnServerCheckTransmit_Register(OnCheckTransmit);

          // Добавить консольную команду для скрытия объекта
          AddConsoleCommand("hide_entity", "Hide an entity by index",
              ConVarFlag.LinkedConcommand | ConVarFlag.Release,
              Command_HideEntity, HookMode.Post);
      }

      public void OnPluginEnd()
      {
          OnServerCheckTransmit_Unregister(OnCheckTransmit);
      }

      private ResultType Command_HideEntity(int caller, CommandCallingContext context, string[] arguments)
      {
          if (arguments.Length < 2)
          {
              PrintToServer("Usage: hide_entity <entity_index>\n");
              return ResultType.Handled;
          }

          int entityIndex = int.Parse(arguments[1]);
          hiddenEntityHandle = EntIndexToEntHandle(entityIndex);

          if (IsValidEntHandle(hiddenEntityHandle))
          {
              PrintToServer($"Entity {entityIndex} will be hidden from all players\n");
          }
          else
          {
              PrintToServer($"Invalid entity index: {entityIndex}\n");
          }

          return ResultType.Handled;
      }

      private void OnCheckTransmit(nint[] infos)
      {
          if (hiddenEntityHandle == -1 || !IsValidEntHandle(hiddenEntityHandle))
              return;

          // Скрыть объект от всех игроков
          for (int playerSlot = 0; playerSlot < infos.Length; playerSlot++)
          {
              ClearTransmitInfoEntity(infos[playerSlot], hiddenEntityHandle);
              // Также очистить из списка постоянной передачи
              ClearTransmitInfoAlways(infos[playerSlot], hiddenEntityHandle);
          }
      }
  }
  [code][/spoiler]
[size=16][b]Пример 2: Создание видимости для конкретных игроков[/b][/size]

Скрывайте объекты от конкретных игроков на основе условий:

[spoiler=c#][/code]csharp
  using Plugify;
  using static s2sdk.s2sdk;
  using System.Collections.Generic;

  public unsafe class PlayerVisibility : Plugin
  {
      // Карта слот игрока -> список скрытых хэндлов объектов
      private Dictionary<int, List<int>> playerHiddenEntities = new();

      public void OnPluginStart()
      {
          OnServerCheckTransmit_Register(OnCheckTransmit);
      }

      public void OnPluginEnd()
      {
          OnServerCheckTransmit_Unregister(OnCheckTransmit);
      }

      // Скрыть конкретный объект от конкретного игрока
      public void HideEntityFromPlayer(int playerSlot, int entityHandle)
      {
          if (!playerHiddenEntities.ContainsKey(playerSlot))
          {
              playerHiddenEntities[playerSlot] = new List<int>();
          }

          if (!playerHiddenEntities[playerSlot].Contains(entityHandle))
          {
              playerHiddenEntities[playerSlot].Add(entityHandle);
          }
      }

      // Показать ранее скрытый объект игроку
      public void ShowEntityToPlayer(int playerSlot, int entityHandle)
      {
          if (playerHiddenEntities.ContainsKey(playerSlot))
          {
              playerHiddenEntities[playerSlot].Remove(entityHandle);
          }
      }

      private void OnCheckTransmit(nint[] infos)
      {
          for (int playerSlot = 0; playerSlot < infos.Length; playerSlot++)
          {
              // Использование ООП стиля
              var info = new CheckTransmitInfo(infos[playerSlot]);

              // Проверить, есть ли у этого игрока скрытые объекты
              if (playerHiddenEntities.ContainsKey(playerSlot))
              {
                  foreach (int entityHandle in playerHiddenEntities[playerSlot])
                  {
                      // Скрывать только если объект всё ещё валиден
                      if (IsValidEntHandle(entityHandle))
                      {
                          info.ClearEntity(entityHandle);
                          info.ClearAlways(entityHandle);
                      }
                  }
              }
          }
      }
  }
  [code][/spoiler]
[size=16][b]Пример 3: Скрыть игроков друг от друга[/b][/size]

Создать "режим невидимости", где конкретные игроки не видят друг друга:

[spoiler=c#][/code]csharp
  using Plugify;
  using static s2sdk.s2sdk;
  using System.Collections.Generic;

  public unsafe class InvisibleMode : Plugin
  {
      private HashSet<int> invisiblePlayers = new();

      public void OnPluginStart()
      {
          OnServerCheckTransmit_Register(OnCheckTransmit);

          AddConsoleCommand("toggle_invisible", "Toggle invisible mode",
              ConVarFlag.LinkedConcommand | ConVarFlag.Release | ConVarFlag.ClientCanExecute,
              Command_ToggleInvisible, HookMode.Post);
      }

      public void OnPluginEnd()
      {
          OnServerCheckTransmit_Unregister(OnCheckTransmit);
      }

      private ResultType Command_ToggleInvisible(int caller, CommandCallingContext context, string[] arguments)
      {
          if (caller == -1) return ResultType.Handled;

          if (invisiblePlayers.Contains(caller))
          {
              invisiblePlayers.Remove(caller);
              PrintToChat(caller, "Invisible mode: OFF");
          }
          else
          {
              invisiblePlayers.Add(caller);
              PrintToChat(caller, "Invisible mode: ON");
          }

          return ResultType.Handled;
      }

      private void OnCheckTransmit(nint[] infos)
      {
          for (int receiverSlot = 0; receiverSlot < infos.Length; receiverSlot++)
          {
              var info = new CheckTransmitInfo(infos[receiverSlot]);

              // Для каждого невидимого игрока
              foreach (int invisibleSlot in invisiblePlayers)
              {
                  // Не скрывать от самих себя
                  if (invisibleSlot == receiverSlot)
                      continue;

                  // Получить хэндл объекта невидимого игрока
                  int invisibleHandle = PlayerSlotToEntHandle(invisibleSlot);

                  if (IsValidEntHandle(invisibleHandle))
                  {
                      // Скрыть невидимого игрока от этого получателя
                      info.ClearEntity(invisibleHandle);
                      info.ClearAlways(invisibleHandle);
                  }
              }
          }
      }
  }
  [code][/spoiler]
[size=16][b]Пример 4: Показывать только членов команды[/b][/size]

Отображать объекты только игрокам из той же команды:

[spoiler=c#][/code]csharp
  using Plugify;
  using static s2sdk.s2sdk;
  using System.Collections.Generic;

  public unsafe class TeamVisibility : Plugin
  {
      // Карта хэндлов объектов к номерам команд
      private Dictionary<int, int> teamEntities = new();

      public void OnPluginStart()
      {
          OnServerCheckTransmit_Register(OnCheckTransmit);
      }

      public void OnPluginEnd()
      {
          OnServerCheckTransmit_Unregister(OnCheckTransmit);
      }

      // Зарегистрировать объект как видимый только для конкретной команды
      public void SetEntityTeamVisibility(int entityHandle, int team)
      {
          teamEntities[entityHandle] = team;
      }

      private void OnCheckTransmit(nint[] infos)
      {
          for (int playerSlot = 0; playerSlot < infos.Length; playerSlot++)
          {
              var info = new CheckTransmitInfo(infos[playerSlot]);

              // Получить команду игрока
              int playerHandle = PlayerSlotToEntHandle(playerSlot);
              if (!IsValidEntHandle(playerHandle))
                  continue;

              int playerTeam = GetEntityTeam(playerHandle);

              // Проверить каждый объект с ограничением по команде
              foreach (var kvp in teamEntities)
              {
                  int entityHandle = kvp.Key;
                  int requiredTeam = kvp.Value;

                  if (!IsValidEntHandle(entityHandle))
                      continue;

                  // Скрыть объект, если игрок не в правильной команде
                  if (playerTeam != requiredTeam)
                  {
                      info.ClearEntity(entityHandle);
                      info.ClearAlways(entityHandle);
                  }
                  else
                  {
                      // Убедиться, что он виден для членов команды
                      info.SetEntity(entityHandle);
                  }
              }
          }
      }

      private int GetEntityTeam(int entityHandle)
      {
          // Предполагая, что у вас есть способ получить номер команды из объекта
          // Это просто пример - реализуйте на основе вашего доступа к схеме
          return GetEntSchemaInt32(entityHandle, "CBaseEntity", "m_iTeamNum", false, 0);
      }
  }
  [code][/spoiler]
[size=16][b]Пример 5: Контроль полного обновления[/b][/size]

Принудительно отправлять полные обновления для конкретных ситуаций:

[spoiler=c#][/code]csharp
  using Plugify;
  using static s2sdk.s2sdk;
  using System.Collections.Generic;

  public unsafe class FullUpdateControl : Plugin
  {
      private HashSet<int> playersNeedingFullUpdate = new();

      public void OnPluginStart()
      {
          OnServerCheckTransmit_Register(OnCheckTransmit);

          // Подключить спавн игрока для принудительного полного обновления
          HookEvent("player_spawn", Event_PlayerSpawn, HookMode.Post);
      }

      public void OnPluginEnd()
      {
          OnServerCheckTransmit_Unregister(OnCheckTransmit);
      }

      private void Event_PlayerSpawn(string name, nint @event, bool dontBroadcast)
      {
          int playerSlot = GetEventInt(@event, "userid", 0);

          // Отметить игрока для полного обновления при следующей передаче
          playersNeedingFullUpdate.Add(playerSlot);
      }

      private void OnCheckTransmit(nint[] infos)
      {
          for (int playerSlot = 0; playerSlot < infos.Length; playerSlot++)
          {
              var info = new CheckTransmitInfo(infos[playerSlot]);

              // Проверить, нужно ли этому игроку полное обновление
              if (playersNeedingFullUpdate.Contains(playerSlot))
              {
                  info.SetFullUpdate(true);

                  // Удалить после однократного применения
                  playersNeedingFullUpdate.Remove(playerSlot);

                  PrintToServer($"Sending full update to player {playerSlot}\n");
              }
          }
      }
  }
  [font=monospace]`[/font]
[/spoiler]
[size=20][b]Справочник методов[/b][/size]

[size=16][b]Операции с Entity BitVec[/b][/size]

| Метод | Описание |
|--------|-------------|
| [font=monospace]SetEntity(entityHandle)[/font] | Отметить объект как передаваемый этому игроку |
| [font=monospace]ClearEntity(entityHandle)[/font] | Отметить объект как не передаваемый этому игроку |
| [font=monospace]IsEntitySet(entityHandle)[/font] | Проверить, отмечен ли объект для передачи |
| [font=monospace]SetEntityAll()[/font] | Отметить все объекты как передаваемые |
| [font=monospace]ClearEntityAll()[/font] | Отметить все объекты как не передаваемые |

[size=16][b]Операции с Non-Player BitVec[/b][/size]

| Метод | Описание |
|--------|-------------|
| [font=monospace]SetNonPlayer(entityHandle)[/font] | Отметить не-игрока как передаваемого |
| [font=monospace]ClearNonPlayer(entityHandle)[/font] | Отметить не-игрока как не передаваемого |
| [font=monospace]IsNonPlayerSet(entityHandle)[/font] | Проверить, отмечен ли не-игрок для передачи |
| [font=monospace]SetNonPlayerAll()[/font] | Отметить всех не-игроков как передаваемых |
| [font=monospace]ClearNonPlayerAll()[/font] | Отметить всех не-игроков как не передаваемых |

[size=16][b]Операции постоянной передачи[/b][/size]

| Метод | Описание |
|--------|-------------|
| [font=monospace]SetAlways(entityHandle)[/font] | Принудительно всегда передавать объект |
| [font=monospace]ClearAlways(entityHandle)[/font] | Снять флаг принудительной передачи |
| [font=monospace]IsAlwaysSet(entityHandle)[/font] | Проверить, принудительно ли передается объект |
| [font=monospace]SetAlwaysAll()[/font] | Принудительно всегда передавать все объекты |
| [font=monospace]ClearAlwaysAll()[/font] | Очистить все флаги принудительной передачи |

[size=16][b]Операции с целевыми слотами[/b][/size]

| Метод | Описание |
|--------|-------------|
| [font=monospace]GetTargetSlotsCount()[/font] | Получить количество целевых слотов игроков |
| [font=monospace]GetTargetSlot(index)[/font] | Получить конкретный целевой слот по индексу |
| [font=monospace]AddTargetSlot(playerSlot)[/font] | Добавить целевой слот игрока |
| [font=monospace]RemoveTargetSlot(index)[/font] | Удалить целевой слот по индексу |
| [font=monospace]GetTargetSlotsAll()[/font] | Получить массив всех целевых слотов |
| [font=monospace]RemoveTargetSlotsAll()[/font] | Очистить все целевые слоты |

[size=16][b]Операции полного обновления[/b][/size]

| Метод | Описание |
|--------|-------------|
| [font=monospace]GetFullUpdate()[/font] | Получить флаг полного обновления |
| [font=monospace]SetFullUpdate(bool)[/font] | Установить флаг полного обновления |

[quote][b]Info:[/b] [b]Примечание[/b]: Слот игрока определяется индексом массива, а не методом. Используйте индекс цикла для определения, с каким слотом игрока вы работаете.[/quote]

[size=20][b]Лучшие практики[/b][/size]

[quote][b]Info:[/b] - Кэшируйте хэндлы объектов вне колбэка CheckTransmit, когда это возможно
[list]
[*]Проверяйте хэндлы перед их использованием в CheckTransmit
[*]Избегайте дорогих операций в колбэке (вызывается каждый тик!)
[*]Используйте ранние возвраты, чтобы пропустить ненужную обработку[/quote]
[/list]

[list=1]
[*]✅ [b]Всегда регистрируйте во время OnPluginStart[/b] и отменяйте регистрацию во время OnPluginEnd
[*]✅ [b]Проверяйте хэндлы объектов[/b] перед использованием их в операциях передачи
[*]✅ [b]Делайте логику колбэка минимальной[/b] - CheckTransmit выполняется каждый тик
[*]✅ [b]Используйте ООП обёртку[/b] для более чистого и читаемого кода (когда доступно)
[*]✅ [b]Кэшируйте часто используемые данные[/b] вне колбэка
[*]❌ [b]Не выполняйте тяжёлые вычисления[/b] в колбэке CheckTransmit
[*]❌ [b]Не забывайте отменять регистрацию[/b] - могут возникнуть утечки памяти и сбои
[*]❌ [b]Не предполагайте, что объекты валидны[/b] - всегда проверяйте хэндлы
[/list]

[size=20][b]Типичные случаи использования[/b][/size]

[size=16][b]Системы наблюдателей[/b][/size]
Скрывайте игроков от наблюдателей или создавайте пользовательские виды наблюдателей, контролируя, какие объекты могут видеть наблюдатели.

[size=16][b]Невидимость администратора[/b][/size]
Создайте режим невидимого администратора, где администраторы могут передвигаться, не будучи замеченными игроками.

[size=16][b]Командный геймплей[/b][/size]
Реализуйте туман войны или видимость для конкретных команд в пользовательских игровых режимах.

[size=16][b]Оптимизация объектов[/b][/size]
Сокращайте сетевой трафик, предотвращая передачу далёких или неактуальных объектов.

[size=16][b]Пользовательские системы зрения[/b][/size]
Создавайте ночное зрение, тепловидение или другие специальные режимы зрения, контролируя видимость объектов.

[size=20][b]Устранение неполадок[/b][/size]

[size=16][b]Объекты всё ещё видны после очистки[/b][/size]
[b]Проблема[/b]: [font=monospace]ClearEntity()[/font] вызван, но объект всё ещё виден
[b]Решение[/b]: Также вызовите [font=monospace]ClearAlways()[/font] - объект может быть в списке постоянной передачи

[size=16][b]Проблемы с производительностью[/b][/size]
[b]Проблема[/b]: Задержки сервера после реализации CheckTransmit
[b]Решение[/b]: Профилируйте ваш колбэк - вероятно, выполняется слишком много работы за тик. Кэшируйте данные и минимизируйте операции.

[size=16][b]Изменения не применяются[/b][/size]
[b]Проблема[/b]: Модификации передачи, похоже, не работают
[b]Решение[/b]: Убедитесь, что вы изменяете правильный CheckTransmitInfo для целевого игрока. Проверьте, что хэндлы объектов валидны.

[size=16][b]Хук не работает[/b][/size]
[b]Проблема[/b]: Колбэк никогда не вызывается
[b]Решение[/b]: Проверьте, что вы зарегистрировали хук во время [font=monospace]OnPluginStart[/font] и что плагин S2SDK загружен.

[size=20][b]Заключение[/b][/size]

Система CheckTransmit - это мощный инструмент для контроля видимости объектов в Source 2. Подключившись к решениям сервера о передаче, вы можете создавать сложные игровые механики, оптимизировать сетевой трафик и создавать уникальный игровой опыт.

Ключевые моменты:
[list]
[*]Регистрируйте хуки в [font=monospace]OnPluginStart[/font], отменяйте регистрацию в [font=monospace]OnPluginEnd[/font]
[*]Используйте функциональный или ООП API в зависимости от предпочтений
[*]Делайте колбэки эффективными - они выполняются каждый тик
[*]Всегда проверяйте хэндлы объектов перед использованием
[*]Очищайте и флаги Entity, и Always при скрытии объектов
[/list]

[quote][b]Info:[/b] [b]Дополнительная литература[/b]: Ознакомьтесь с руководством [url=./8.handle-system]Handle System[/url] для получения дополнительной информации о работе с хэндлами объектов и слотами игроков.[/quote]
