{
  "name": "plugify-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "check:manifests": "node scripts/check-manifests.mjs",
    "check:i18n": "node scripts/check-i18n-parity.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check": "npm run check:manifests && npm run check:i18n && npm run build && npm run check:links"
  },
  "dependencies": {
    "@nuxtjs/robots": "^5.2.6",
    "@nuxtjs/sitemap": "^7.2.7",
    "nuxt": "^3.15.4",
    "shadcn-docs-nuxt": "^1.1.7",
    "simple-icons": "^14.5.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
